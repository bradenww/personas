.DS_Store
temp/

# Source material (copyrighted transcripts) — never committed
sources/

# All build artifacts and source material live here — never committed
working-files/

# Raw source material (often copyrighted) — never committed
sources/
